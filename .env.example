# Gateway'in dış dünyayı dinleyeceği port
LISTEN_PORT=5060

# DİKKAT: 'cargo run' ile yerel geliştirme yaparken,
# hedef servisimiz (sip-signaling) Docker içinde 'localhost' üzerinden
# erişilebilir olacaktır. Bu yüzden buraya 'localhost' YERİNE,
# belirsizliği ortadan kaldırmak için doğrudan '127.0.0.1' yazıyoruz.
TARGET_HOST=127.0.0.1

# Hedef servisin dinlediği port
TARGET_PORT=5060

# Log seviyesi için: RUST_LOG=debug